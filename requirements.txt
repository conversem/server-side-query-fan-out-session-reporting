# Public release requirements (open source, no GCP-only dependencies)
# For local SQLite-only: pip install .
# For local BigQuery modes: pip install ".[gcp]"
# For ML session refinement: pip install ".[ml]"
# For all development: pip install ".[dev,ml,monitoring,viz]"

# Cloudflare SDK
cloudflare>=3.0.0

# Security
defusedxml>=0.7.1

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Excel export
openpyxl>=3.1.0

# Utilities
python-dateutil>=2.8.2
pyyaml>=6.0.0
httpx>=0.24.0
requests>=2.28.0

# ML (session refinement, semantic embeddings)
scikit-learn>=1.3.0
